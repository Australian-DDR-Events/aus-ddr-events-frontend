import { Form } from 'antd';
import styled, { defaultSpacing } from 'types/styled-components';

// eslint-disable-next-line import/prefer-default-export
export const StyledForm = styled(Form)`
  max-width: ${defaultSpacing * 88}px;
  padding: 16px;
`;
