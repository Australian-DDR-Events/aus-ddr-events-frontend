import { Typography } from 'antd';
import styled from 'types/styled-components';

// eslint-disable-next-line import/prefer-default-export
export const WrapText = styled(Typography.Text)`
  white-space: normal;
`;
