query getDancerById($dancerId: ID!) {
  dancerById(id: $dancerId) {
    ...DancerFields
  }
}

fragment DancerFields on Dancer {
  id
  ddrCode
  ddrName
  primaryMachineLocation
  profilePictureUrl
  state
}
