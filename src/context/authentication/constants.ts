import { AuthenticationUser } from './types';

// eslint-disable-next-line import/prefer-default-export
export const DefaultAuthenticationUser: AuthenticationUser = {
  id: '',
  hasVerifiedEmail: false,
};
